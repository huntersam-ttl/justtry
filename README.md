# Just Try Media

Static clone of `https://just-try-media--hunter2324.replit.app`.

## Run locally

```sh
npm install
npm run dev
```

The app is a Vite-built static site. The downloaded JavaScript and CSS bundles live in `assets/`.
